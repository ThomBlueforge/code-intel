"""Phase 2 — Tree-sitter parsing.

Deterministic extraction of code symbols (functions, classes, interfaces,
etc.) from source files. No LLM is ever involved here; Tree-sitter produces the
structural facts that every later phase builds on.
"""
