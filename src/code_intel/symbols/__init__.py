"""Phase 4 — symbol index.

Fast exact / prefix / fuzzy symbol search over the deterministic symbol table,
with language, type, and path filters. Deliberately lightweight: it selects
only the columns needed to rank and display, never the full source ``code``.
"""
